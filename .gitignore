node_modules/
dist/
*.tsbuildinfo

# Source audio, generated segments, artwork, peaks, and the SQLite store.
# Never committed: large, binary, and the library is not ours to redistribute.
media/

# Secrets: DNS API token, event code, DJ credential (D12).
.env
.env.*
!.env.example

# Caddy's account key, certificates and access log. Generated, and the account
# key is a credential (D12).
ops/caddy-data/
ops/Caddyfile

# Runtime logs from ops/scripts/disco.sh
ops/logs/
